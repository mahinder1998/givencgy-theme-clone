<header class="font-mavenReg fixed top-0 w-full">
    <!-- top bar -->
    <div class="bg-black py-4 px-3 text-white text-center uppercase text-xs">
        <span>Complementry Gift Wrapping</span>
    </div>

    <!-- main nav -->
    <div class="relative bg-white">
        <!-- container-->
        <div class="container mx-auto"> 
            <div class="p-5 px-2 grid  gap-2 grid-cols-[60px,1fr,60px]
            lg:grid-cols-[1fr,180px,1fr]
            items-center
            ">
                <!-- small devie left col -->
                <div class="header-left grid grid-cols-2 gap-3 items-center lg:hidden ">
                    <div class="menu-toggle">
                        {% render 'menu-icon' %}
                    </div>
                    <div class="search-toggle justify-self-end">
                        {% render 'search-icon' %}
                    </div>
                </div>
                <!-- large device left col-->
                <div class="hidden lg:block">
                    <div class="grid grid-flow-col gap-2 text-xs  uppercase lg:place-content-start lg:gap-4">
                        <div class="country-code">
                            <b>UAE(AED) </b>
                         </div>
                         <div class="customer-support">
                             Can we help you ?
                         </div>
                         <div class="contact-info">
                             9816975832
                         </div>
                         <div class="lang-switcher">
                             Language: EN
                         </div>
                    </div>
                </div>

                <div class="web-logo place-self-center">
                    <a href="{{shop.url}}" >
                    {% render 'logo' %}
                    </a>
                </div>
                <div class="header-right relative">
                    <div class="grid grid-flow-col gap-2 lg:gap-3 lg:place-content-end">
                    <div class="customer-toggle">
                        {% render 'user-icon' %}
                    </div>
                    <div class="hidden lg:block search-toggle">
                        {% render 'search-icon' %}
                    </div>
                    <div class="hidden lg:block search-toggle">
                        {% render 'heart-icon' %}
                    </div>
                    <div class="cart-toggle relative">
                        {% render 'bag-icon' %}
                        <span class="absolute top-[-14px] right-0 bg-black rounded-full w-4 h-4 text-white text-center leading-4 text-xs">0</span>
                    </div>
                    
                    <div class="menu-close hidden">
                        {% render 'close-icon' %}
                    </div>

                    </div>
                </div>
            </div>


        <!-- Mobile menu -->
        <div class="mobile-menu-wrap bg-white absolute w-full overflow-y-auto h-[calc(100vh-112px)] hidden">
            <div class="mobile-nav p-4 border-b ">
                <ul class="grid gap-4">
                    <li><a href="#" class="font-semibold uppercase text-sm">New</a></li>
                    <li><a href="#" class="font-semibold uppercase text-sm">Excliusive Services</a></li>
                    <li class="grid grid-cols-[1fr,20px] gap-4 items-center">
                        <a href="#" class="font-semibold uppercase text-sm">Fragrance</a>
                        <button>
                            {% render 'arrow-right' %}
                        </button>    
                    </li>
                    <li class="grid grid-cols-[1fr,20px] gap-4 items-center"><a href="#" class="font-semibold uppercase text-sm">La Collection Particuler</a> 
                    <button>
                        {% render 'arrow-right' %}
                    </button>  </li>
                    <li class="grid grid-cols-[1fr,20px] gap-4 items-center"><a href="#" class="font-semibold uppercase text-sm">Make Up</a> {% render 'arrow-right' %}</button></li>
                    <li class="grid grid-cols-[1fr,20px] gap-4 items-center"><a href="#" class="font-semibold uppercase text-sm">Skincare</a>
                    <button>
                        {% render 'arrow-right' %}
                    </button>  </li>
                    <li><a href="#" class="font-semibold uppercase text-sm">Couture</a></li>
                </ul>
            </div>

            <div class="mobile-nav-footer p-4">
                <ul class="grid gap-4">
                    <li>
                       <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                        {% render 'user-icon' %}
                        <span>
                            My Account
                           </span>
                       </a> 
                    </li>
                    <li>
                       <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                        {% render 'chat-icon' %}
                        <span>
                            Can we help You ?
                           </span>
                       </a> 
                    </li>
                    <li>
                        <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                         {% render 'phone-icon' %}
                         <span>
                             Customer Services 9816975832
                            </span>
                        </a> 
                     </li>
                     <li>
                        <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                         {% render 'envelop-icon' %}
                         <span>
                             Newsletter
                            </span>
                        </a> 
                     </li>
                     <li>
                        <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                         {% render 'heart-icon' %}
                         <span>
                             Wishlist
                            </span>
                        </a> 
                     </li>
                     <li>
                        <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                         {% render 'location-icon' %}
                         <span>
                             Your Store
                            </span>
                        </a> 
                     </li>

                     <li class="grid grid-cols-[1fr,20px] gap-4 items-center">
                        <a href="#">
                        Language EN
                        </a> 
                     <button>
                         {% render 'arrow-right' %}
                     </button>  </li>
                     <li class="grid grid-cols-[1fr,20px] gap-4 items-center">
                        <a href="#" class="grid grid-cols-[20px,1fr] gap-3 items-center">
                            {% render 'global-icon' %}
                            <span class="font-semibold text-sm">
                                UAE (AED)
                               </span>
                           </a> 
                     <button>
                         {% render 'arrow-right' %}
                     </button>  </li>

                </ul>
            </div>

        </div>


        </div>
    </div>


</header>